# 安全策略

## 威胁模型

vococo 是**单用户个人 AI 助理**,不是多租户 SaaS。默认 `bypassPermissions`(工具自动执行,不逐次问),换取手机端低摩擦的使用体验。这意味着:

- 能操作 agent 的人 = 拥有者本人,不存在"用户 A 不该看到用户 B 数据"这类多租户隔离需求。
- **没有沙箱**:agent 以运行该进程的系统用户身份直接读写文件、执行命令,和你自己在终端里手动操作的权限完全一样。所有下面列的防护都是在这个前提上加的减速带,不是牢笼。
- 真正的攻击面是 **prompt injection**:agent 处理不可信的第三方内容(转发消息、抓取的网页、读到的文件)时被藏在内容里的指令带偏,替攻击者干活。

## 已有的防护层

代码里的判定逻辑见 [`vococo/tools/danger.py`](vococo/tools/danger.py),概念说明见 [CONTEXT.md](CONTEXT.md) 的「危险分级 / 审批闸」。简述:

| 层 | 做法 |
|---|---|
| 三档风险分级 | 每次工具调用判成 `allow`/`escalate`/`block`;灾难级命令(删根、格式化、fork 炸弹)直接拦,危险但非灾难的操作(`git push`、装包、`curl\|sh`、越界写文件)请你在线批准 |
| 越界写入硬拦 | worktree 会话写到共享主仓库外、在 Claude Code 记忆目录乱建文件 → 一律拒绝,不受开关控制 |
| 密钥收敛 | 自用 secret 从子进程可见的环境变量里清掉;命令里同时出现"密钥变量名 + 外传渠道"会被拦 |
| 敏感读取标注 | 读到 SSH 私钥、云凭据等文件时打日志(不拦截,避免误伤正常运维) |
| 输出侧过滤 | 回复发出前扫一遍已知密钥字面值和常见密钥格式(PEM 私钥块、云厂商 token),命中打码 |
| Web 入口 | 访问口令用常量时间比较防时序攻击;CSP;跨源请求一律拒绝(防 DNS rebinding) |
| MCP 扩展 | 默认禁止从 Web 远程注册本地 stdio 类 MCP(等于允许远程执行任意命令) |

## 已知局限(不是漏洞,是有意识的取舍)

- **黑名单式拦截是减速带,不是边界**:枚举永远不完整,base64 编码、写文件再传、间接引用都能绕过现有的危险命令识别。
- **无沙箱**:根治需要容器/VM 级隔离,目前判断这对个人助理场景的实用性(读写真实文件)代价过高,暂不做,除非威胁模型升级(比如以后要跑不受信的第三方 skill)。
- **第三方模型供应商的 API key 必须留在环境变量里**:Claude Code CLI 的机制要求如此,无法像其他自用密钥一样从 env 清除,只能靠命令层的定向拦截兜底。
- **单用户即审批人**:批准危险操作的就是当前聊天的你自己。如果你的 Web 访问口令被盗,攻击者能做的和你本人一样多——这不是额外的漏洞,而是单用户个人助理这个定位下的固有假设。

## 报告安全问题

请通过本仓库 GitHub 的 **Security → Report a vulnerability**(私有安全公告)提交,不要在公开 Issue 里描述具体的可利用细节。
