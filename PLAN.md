# 会话与任务统一重构实施计划

> 依据：`docs/design/voice-companion/05-session-unification-and-code-reuse-plan.md`
> 当前状态：已获用户确认，按里程碑实施。

## 目标

把文本、按住说话、Omni 语音收敛为同一套会话与任务基础设施，最终只保留一个“对话”入口；实现过程不靠复制新逻辑，而是抽取共享能力并保留语音传输适配器。

## 里程碑

### M1：任务领域中性化

- [x] 把任务查询范围从 `origin=voice/chat` 收敛到 `session_key` / `source`。
- [x] 增加中性 `/tasks*` API，旧 `/voice/tasks*` 只做兼容转发。
- [x] 把任务事件广播从 `voice.notify` 中拆出，避免 `task_runner` 反向依赖 voice。
- [x] 保持 `sdk_task_views` 与后台执行任务分开（现有模型保持不变）。
- [x] 补充任务事件总线测试，并通过既有任务回归测试。

M1 已完成：任务查询、任务事件和前端任务状态流已切到中性接口；旧 `/voice/tasks*` 保留兼容转发。

### M2：统一前台会话轮次

- [ ] 新增小型 `core.conversation` 轮次协调层。
- [ ] 统一 session lock、turn_id、取消、resume、事件和历史落库。
- [ ] 先让 Web 文本和 `/voice/send` 兼容入口共用该层。
- [ ] 保持现有前端行为不变，完成单测和并发测试。

### M3：统一前端输入和时间线

- [ ] 复用主会话现有 composer，不另写语音输入框。
- [ ] 统一文本/语音事件渲染和任务状态条。
- [ ] 语音控制仅保留 Omni、录音、TTS、静音、打断等适配器职责。
- [ ] 浏览器与移动端回归测试。

### M4：统一主会话与历史

- [ ] `voice-chat:main` 迁移到 `main`。
- [ ] 统一清空、历史、标题、模型和思考深度语义。
- [ ] 保留旧 Key 只读兼容和旧入口跳转。
- [ ] 验证刷新、重启、跨设备恢复。

### M5：统一入口与清理

- [ ] 页面默认进入统一“对话”视图。
- [ ] 移除重复的主会话/语音入口。
- [ ] 删除重复 session、turn、history、notify 实现。
- [ ] 更新旧设计文档，执行移除演练。

## 当前执行方式

先完成 M1 的最小垂直切片：任务范围查询与中性 API；每个小步先读现状、写测试、实现、跑单测，再进入下一小步。任何需要改变会话模型的动作，先更新本计划和设计文档，不在代码里临时转向。
